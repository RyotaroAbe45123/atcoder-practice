#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin >> s;
    int res = 0;
    res += (s[0] - '0');
    res += (s[1] - '0');
    res += (s[2] - '0');
    cout << 111 * res << endl;
}
